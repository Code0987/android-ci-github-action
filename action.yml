name: 'android-ci-github-action'
description: >-
  DEPRECATED: Unmaintained Docker action for Android CI (base image last updated 2020).
  Migrate to actions/setup-java, android-actions/setup-android, and gradle/actions/setup-gradle.
author: 'code0987 <durgapalneeraj@gmail.com>'
branding:
  icon: 'alert-triangle'
  color: 'yellow'

inputs:
  args:
    description: 'Shell commands to run inside the legacy Android CI container (deprecated).'
    required: true

runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.args }}
